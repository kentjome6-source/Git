// Temporarily disabled - install laravel-echo and pusher-js if needed
// import Echo from 'laravel-echo';
// import Pusher from 'pusher-js';

// Minimal bootstrap for now - Echo configuration can be added back later
console.log('Bootstrap loaded');
